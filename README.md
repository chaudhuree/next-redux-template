## Run the project by below way

- first run ```npm install``` in root folder
- then run ```npm run dev``` in root folder
- open another terminal
- run , ```npm install``` in that terminal
- run , ```npm run dev``` in that terminal